## Tests

- After modifying any `.gd` files, run `./scripts/gdscript_checks.sh`.
- Treat `./scripts/gdscript_checks.sh` as the main test command for this project.
- If `./scripts/gdscript_checks.sh` fails, fix all reported issues before considering the task complete.
